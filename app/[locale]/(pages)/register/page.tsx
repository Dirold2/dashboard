"use client";

import { FormRegister } from "@cmp/Form";

const RegisterPage = (): JSX.Element => {

    return (
        <main>
            <FormRegister />
        </main>
    );
};

export default RegisterPage;